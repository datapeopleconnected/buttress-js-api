{
  "name": "data-filter-user",
  "selection": {
    "role": {
      "@eq": "user"
    }
  },
  "config": [{
    "verbs": ["GET", "SEARCH", "PUT", "POST", "DELETE"],
    "schema": ["%ALL%"],
    "query": {
      "access": "%FULL_ACCESS%"
    }
  }]
}